export const REG_POSITIVE_INTEGER = '^[0-9]{1,}$'
export const REG_ARRAY_OF_PATTERNS = '^((((([a-z]+[*]{0,1})|[*]{1})[.]?)+)[;]?)+$'
export const REG_INVITE_CODE = '^[a-zA-Z0-9]{5,20}$'

export default {
    REG_POSITIVE_INTEGER,
    REG_ARRAY_OF_PATTERNS,
    REG_INVITE_CODE,
}
